<Window x:Class="HebrewTaskbarWidget.ZmanEditDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="עריכת זמן"
        FlowDirection="RightToLeft"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        Topmost="True"
        KeyDown="Window_KeyDown">

    <Window.Resources>
        <SolidColorBrush x:Key="AccentBrush" Color="#1A5FB4" />
        <SolidColorBrush x:Key="ControlBorderBrush" Color="#D5D5D8" />
        <SolidColorBrush x:Key="ControlBackgroundBrush" Color="#FFFFFF" />
        <SolidColorBrush x:Key="PrimaryTextBrush" Color="#1B1C1F" />
        <SolidColorBrush x:Key="SecondaryTextBrush" Color="#6B6D73" />

        <!-- תיבות טקסט מעוגלות - תואם לעיצוב הכללי (חלוניות מעוגלות, כפתורים
             מעוגלים) יותר מהתיבה המרובעת ה"גולמית" של WPF כברירת מחדל. -->
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="{StaticResource ControlBackgroundBrush}" />
            <Setter Property="Foreground" Value="{StaticResource PrimaryTextBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource ControlBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="8,2" />
            <Setter Property="FontSize" Value="12.5" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border CornerRadius="6" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}" VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ComboBox - נשאר בעיצוב הסטנדרטי (כמו בשאר פאנל ההגדרות), רק
             מרווחים/גופן תואמים - אין צורך לשכתב את כל ה-ControlTemplate
             המורכב (כולל חץ/Popup) בשביל חלונית קטנה כזו. -->
        <Style TargetType="ComboBox">
            <Setter Property="Padding" Value="8,5" />
            <Setter Property="FontSize" Value="12.5" />
        </Style>

        <!-- כפתורי פעולה מעוגלים - תואם ל-ActionButtonStyle/PrimaryActionButtonStyle
             בפאנל ההגדרות עצמו (אותו CornerRadius, אותה התנהגות Hover/Press). -->
        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="32" />
            <Setter Property="MinWidth" Value="80" />
            <Setter Property="Padding" Value="14,0" />
            <Setter Property="FontSize" Value="12.5" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Background" Value="{StaticResource ControlBackgroundBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource ControlBorderBrush}" />
            <Setter Property="Foreground" Value="{StaticResource PrimaryTextBrush}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" CornerRadius="7" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Opacity" Value="0.85" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="Opacity" Value="0.7" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="PrimaryActionButtonStyle" TargetType="Button" BasedOn="{StaticResource ActionButtonStyle}">
            <Setter Property="Background" Value="{StaticResource AccentBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="FontWeight" Value="SemiBold" />
        </Style>

        <!-- כפתור סמליל קטן ומעוגל (מחיקה) - תואם ל-IconActionButtonStyle בפאנל ההגדרות. -->
        <Style x:Key="IconActionButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="30" />
            <Setter Property="Height" Value="30" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Background" Value="{StaticResource ControlBackgroundBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource ControlBorderBrush}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" CornerRadius="7" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Opacity" Value="0.8" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="Opacity" Value="0.65" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- מתג הפעלה/כיבוי מעוגל (מסלול + כדור נע) - תואם בדיוק ל-
             ToggleSwitchStyle בפאנל ההגדרות עצמו ("הוסף שורת זמן כפולה"). -->
        <Style x:Key="ToggleSwitchStyle" TargetType="CheckBox">
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Foreground" Value="{StaticResource PrimaryTextBrush}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <StackPanel Orientation="Horizontal">
                            <ContentPresenter VerticalAlignment="Center" Margin="0,0,8,0" />
                            <Border x:Name="SwitchTrack" Width="34" Height="18" CornerRadius="9"
                                    Background="{StaticResource ControlBorderBrush}" VerticalAlignment="Center">
                                <Border x:Name="SwitchThumb" Width="14" Height="14" CornerRadius="7"
                                        Background="White" HorizontalAlignment="Left" Margin="2,0,0,0" />
                            </Border>
                        </StackPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="SwitchTrack" Property="Background" Value="{StaticResource AccentBrush}" />
                                <Setter TargetName="SwitchThumb" Property="HorizontalAlignment" Value="Right" />
                                <Setter TargetName="SwitchThumb" Property="Margin" Value="0,0,2,0" />
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="0.85" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border x:Name="RootBorder" CornerRadius="14" Background="{StaticResource ControlBackgroundBrush}" BorderBrush="{StaticResource ControlBorderBrush}" BorderThickness="1" Width="360">
        <Border.Effect>
            <DropShadowEffect BlurRadius="24" ShadowDepth="4" Opacity="0.28" Color="Black" />
        </Border.Effect>

        <StackPanel Margin="22">
            <TextBlock x:Name="TitleText" FontWeight="Bold" FontSize="14.5" TextWrapping="Wrap" Foreground="{StaticResource PrimaryTextBrush}" />

            <TextBlock Text="שם מותאם אישית" Margin="0,16,0,4" FontSize="11" Foreground="{StaticResource SecondaryTextBrush}" />
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBox x:Name="CustomNameTextBox" Grid.Column="0" MinWidth="240" Height="34" VerticalContentAlignment="Center" HorizontalAlignment="Stretch" Margin="10,0,10,0" />
                <Button x:Name="PrimaryDeleteButton" Grid.Column="1" Content="🗑" Style="{StaticResource IconActionButtonStyle}"
                        ToolTip="מחק שורה זו (השורה הכפולה תישאר לבדה)"
                        Visibility="Collapsed" Click="PrimaryDeleteButton_Click" />
            </Grid>
            <TextBlock x:Name="PrimaryMethodText" FontSize="11" Margin="0,6,0,0" Foreground="{StaticResource SecondaryTextBrush}" Visibility="Collapsed" />
            <TextBlock x:Name="PrimaryTimeText" FontSize="11" Margin="0,3,0,0" Foreground="{StaticResource SecondaryTextBrush}" />

            <!-- מוצג לזמנים "רגילים" (לא הדלקת נרות) -->
            <StackPanel x:Name="MethodPanel" Margin="0,16,0,0">
                <TextBlock Text="שיטת חישוב" FontSize="11" Margin="0,0,0,4" Foreground="{StaticResource SecondaryTextBrush}" />
                <ComboBox x:Name="MethodComboBox" SelectionChanged="MethodComboBox_SelectionChanged">
                    <ComboBoxItem Content="השיטה הכללית" />
                    <ComboBoxItem Content="רגילה (זווית שמש)" />
                    <ComboBoxItem Content="72 דקות זמניות" />
                </ComboBox>
            </StackPanel>

            <!-- מוצג רק ל"הדלקת נרות" -->
            <StackPanel x:Name="CandleLightingMinutesPanel" Orientation="Horizontal" Margin="0,16,0,0">
                <TextBlock Text="דקות לפני השקיעה" VerticalAlignment="Center" FontSize="12.5" Foreground="{StaticResource PrimaryTextBrush}" />
                <TextBox x:Name="CandleLightingMinutesTextBox" Width="44" Margin="8,0,0,0" MaxLength="2" TextAlignment="Center" />
            </StackPanel>

            <!-- מוצג רק ל"צאת הכוכבים" -->
            <StackPanel x:Name="TzeitMinutesPanel" Margin="0,16,0,0">
                <TextBlock Text="דקות לאחר שקיעת החמה (ריק = לפי שיטת החישוב)" TextWrapping="Wrap" FontSize="11" Margin="0,0,0,4" Foreground="{StaticResource SecondaryTextBrush}" />
                <TextBox x:Name="TzeitMinutesTextBox" Width="60" HorizontalAlignment="Left" TextAlignment="Center" />
            </StackPanel>

            <!-- זמן כפול - לא מוצג ל"הדלקת נרות" -->
            <StackPanel x:Name="DuplicateSectionPanel" Margin="0,18,0,0">
                <Border BorderBrush="{StaticResource ControlBorderBrush}" BorderThickness="0,1,0,0" Margin="0,0,0,16" />

                <CheckBox x:Name="DuplicateToggle" Content="הוסף שורת זמן כפולה" Style="{StaticResource ToggleSwitchStyle}" Checked="DuplicateToggle_CheckedChanged" Unchecked="DuplicateToggle_CheckedChanged" />

                <StackPanel x:Name="DuplicateDetailsPanel" Margin="0,14,0,0">
                    <TextBlock Text="שם השורה הכפולה" FontSize="11" Margin="0,0,0,4" Foreground="{StaticResource SecondaryTextBrush}" />
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBox x:Name="DuplicateNameTextBox" Grid.Column="0" MinWidth="240" Height="34" VerticalContentAlignment="Center" HorizontalAlignment="Stretch" Margin="10,0,10,0" />
                        <Button x:Name="DeleteDuplicateButton" Grid.Column="1" Content="🗑" Style="{StaticResource IconActionButtonStyle}" ToolTip="מחק שורה כפולה זו" Click="DeleteDuplicateButton_Click" />
                    </Grid>
                    <TextBlock x:Name="DuplicateMethodText" FontSize="11" Margin="0,8,0,0" Foreground="{StaticResource SecondaryTextBrush}" />
                    <TextBlock x:Name="DuplicateTimeText" FontSize="11" Margin="0,3,0,0" Foreground="{StaticResource SecondaryTextBrush}" />
                </StackPanel>
            </StackPanel>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,22,0,0">
                <Button x:Name="SaveButton" Content="שמור" Style="{StaticResource PrimaryActionButtonStyle}" Click="SaveButton_Click" />
                <Button x:Name="CancelButton" Content="ביטול" Style="{StaticResource ActionButtonStyle}" Margin="8,0,0,0" Click="CancelButton_Click" />
            </StackPanel>
        </StackPanel>
    </Border>
</Window>
